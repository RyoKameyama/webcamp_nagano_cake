<h1>ジャンル編集</h1>

<h2>ジャンル名</h2>
<%= form_with model: @genre, url:admin_genre_path, local:true do |f| %>
<div>
  <td><%= @genre.name %></td>
  <td><%= f.text_field :name %></td>
  <%= f.submit '変更を保存' %>
<% end %>
</div>