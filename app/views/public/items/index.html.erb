<h1>商品一覧</h1>(全<%= Item.count %> 件)

<% @items.each do |item| %>
<div>
  <p>
  <%= link_to item_path(item) do %>
  <%= attachment_image_tag item, :image, :fill, 60, 40 %>
  <% end %>
  </p>
  <p><%= item.name %></p>
  <p><%= item.price %></p>
</div>

<% end %>

<%= paginate @items %>